name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore src/CustomerApi.sln
    
    - name: Build application
      run: dotnet build src/CustomerApi.sln --configuration Release --no-restore
    
    - name: Run unit tests
      run: dotnet test src/tests/CustomerApi.UnitTests/CustomerApi.UnitTests.csproj --configuration Release --logger "trx;LogFileName=unit-tests.trx" --no-build
    
    - name: Run integration tests
      run: dotnet test src/tests/CustomerApi.IntegrationTests/CustomerApi.IntegrationTests.csproj --configuration Release --logger "trx;LogFileName=integration-tests.trx" --no-build
    
    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: |
          **/unit-tests.trx
          **/integration-tests.trx
    
    - name: Publish test results
      uses: EnricoMi/publish-unit-test-result-action@v2
      if: always()
      with:
        files: '**/**.trx'
        check_name: Test Results

  build-and-push-image:
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/customerapi:latest,${{ secrets.DOCKER_USERNAME }}/customerapi:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  deploy-to-azure:
    runs-on: ubuntu-latest
    needs: build-and-push-image
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    
    - name: Deploy to Azure Container Apps
      uses: azure/container-apps-deploy-action@v1
      with:
        appSourceUrl: ${{ secrets.DOCKER_USERNAME }}/customerapi:${{ github.sha }}
        acrUsername: ${{ secrets.DOCKER_USERNAME }}
        acrPassword: ${{ secrets.DOCKER_PASSWORD }}
        azureCredentials: ${{ secrets.AZURE_CREDENTIALS }}
        containerAppName: customerapi
        resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }}
        imageToBuild: ${{ secrets.DOCKER_USERNAME }}/customerapi:${{ github.sha }}
